package controller;

import SimpleModel.ITurtleModel;
import TracingModel.ITracingTurtleModel;
import java.util.HashMap;
import java.util.Map;

public class FactoryCommand {
//  public static Map<String, ICommand> getKnownCommands(ITracingTurtleModel model, Appendable out){
//    Map<String, ICommand> knownCommands = new HashMap<String, ICommand>();
//    knownCommands.putIfAbsent("move", new MoveCommand(model, out));//TODO: Potential bug?
//    knownCommands.putIfAbsent("turn", new TurnCommand(model, out));
//    knownCommands.putIfAbsent("save", new SaveCommand(model,out));
//    knownCommands.putIfAbsent("retrieve", new RetrieveCommand(model,out));
//    return knownCommands;
//  }
}
